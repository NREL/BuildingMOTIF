# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: BuildingMOTIF Developer Documentation
author: NREL
logo: logo.png

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: force

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: book.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/NREL/BuildingMOTIF
  path_to_book: devdocs  # Optional path to your book, relative to the repository root
  branch: develop  # TODO: switch to release branch?

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  use_issues_button: true
  use_repository_button: true

# for API autodocs added to jupyter book
sphinx:
  extra_extensions:
  - 'sphinx.ext.autodoc'
  - 'sphinx.ext.viewcode'
  - 'sphinx.ext.autosummary'
  config:
    autosummary_generate: true
    add_module_names: false
    autodoc_default_options:
      members: true
      member-order: bysource
